<UserControl x:Class="Fluxtreme.TableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Fluxtreme"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800" Height="100">
	<UserControl.Resources>
	</UserControl.Resources>
	<DockPanel Margin="2, 2, 2, 10">
		<Grid Height="22" DockPanel.Dock="Top" Background="#FF424242">
			<TextBlock x:Name="title" Margin="5,0,0,0" VerticalAlignment="Center" Opacity="0.5"/>
		</Grid>
		<ListView x:Name="contents" ScrollViewer.VerticalScrollBarVisibility="Hidden" ScrollViewer.HorizontalScrollBarVisibility="Hidden" ScrollViewer.CanContentScroll="False" Grid.IsSharedSizeScope="True">
			<ListView.View>
				<GridView x:Name="contentview" AllowsColumnReorder="False" />
			</ListView.View>
		</ListView>
	</DockPanel>
</UserControl>
